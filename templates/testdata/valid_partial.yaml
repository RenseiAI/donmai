apiVersion: v1
kind: PartialTemplate
metadata:
  name: cli-instructions
  description: Linear CLI usage instructions
content: |
  Use `rensei linear` for all Linear operations.
  Do not use Linear MCP tools directly.
